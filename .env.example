# Application Configuration
APP_URL=http://localhost

# Domain Configuration (for SSL)
# Set this to your actual domain name when enabling SSL
DOMAIN=localhost

# Email for SSL certificate notifications
EMAIL=admin@localhost

# Enable SSL (set to true after obtaining certificates)
ENABLE_SSL=false

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=tastyigniter-database
DB_PORT=3306
DB_DATABASE=tastyigniter
DB_USERNAME=tastyigniter
DB_PASSWORD=tastyigniter

# Cache Configuration
CACHE_DRIVER=redis
REDIS_HOST=tastyigniter-redis
